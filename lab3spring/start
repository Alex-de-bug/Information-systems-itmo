for server:

.env (exampl)
.env in front (exampl)
docker-compose down docker-compose up --build


for local:

/~.**rc:
export SECRET=
export LIFETIME=
export POSTGRES_USER=
export POSTGRES_PASSWORD=
export POSTGRES_DB_PORT=
export POSTGRES_DB=
export POSTGRES_DB_HOST=localhost
export REACT_APP_FRONT=http://localhost:3000
export REACT_APP_SERVER=localhost:8081

for all:

create file .evn in root front
